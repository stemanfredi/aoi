# Minimum versions only — let pip pick exact ones.

# Geospatial core
geopandas>=1.0
shapely>=2.0
pyproj>=3.0
rioxarray>=0.13
xarray
netCDF4

# OSM features
osmnx>=2.0

# Plotting
matplotlib

# OpenTopography SRTM fetch
requests

# UI — Jupyter notebook (`jupyter lab designer.ipynb`)
jupyterlab
ipywidgets>=8.0
ipyleaflet>=0.19

# UI — Streamlit web app (`streamlit run app.py`)
streamlit>=1.30
streamlit-folium>=0.18
folium>=0.14
