# Minimum versions only — let pip choose exact ones. Strict version
# pins (matched to one local .venv) used to fight Colab's pre-installed
# stack and break the bootstrap; minimums work everywhere.

# Geospatial core
geopandas>=1.0
shapely>=2.0
pyproj>=3.0
rioxarray>=0.13
xarray
netCDF4

# OSM features
osmnx>=2.0

# Plotting
matplotlib

# Interactive designer (local Jupyter)
jupyterlab
ipywidgets>=8.0
ipyleaflet>=0.19

# OpenTopography SRTM fetch
requests
