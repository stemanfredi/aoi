# Minimum versions only — let pip choose exact ones. Strict version
# pins (matched to one local .venv) used to fight Colab's pre-installed
# stack and break the bootstrap; minimums work everywhere.

# Geospatial core
geopandas>=1.0
shapely>=2.0
pyproj>=3.0
rioxarray>=0.13
xarray
netCDF4

# OSM features
osmnx>=2.0

# Plotting
matplotlib

# Interactive designer.
# ipywidgets stays on the 7.x protocol (@jupyter-widgets/controls@1.x)
# because Colab's custom widget manager bundle is still on that
# version; ipywidgets 8 uses the @2.x protocol and renders blank in
# Colab. ipyleaflet 0.19+ requires ipywidgets 8, so it follows.
# We use no 7→8 breaking features.
jupyterlab
ipywidgets>=7.7,<8
ipyleaflet>=0.17,<0.19

# OpenTopography SRTM fetch
requests
